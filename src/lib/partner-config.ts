// ============================================================================
// Partner Hub branding — one place to name the partner
// ============================================================================
// The Partner Hub is deliberately NOT Bratt-branded (no mascot, no orange, no
// internal nav) so the landscaping company's sales team sees a neutral pricing
// tool rather than our internal dashboard. Change the values below when the
// partnership is signed and you know how they want to be named.
// ============================================================================

export const PARTNER = {
  /** Shown in the header and the browser tab. */
  name: 'Partner Hub',
  /** One-line description under the hub title. */
  tagline: 'Plant Health Care pricing',
  /** Who to contact with pricing questions. Shown on the calculator page. */
  contactName: 'Connor',
  contactEmail: 'connor@bratttree.com',
} as const;
