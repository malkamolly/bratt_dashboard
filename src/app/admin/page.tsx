export default function AdminPage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-12">
      <p className="bt-eyebrow">Admin</p>
      <h1 className="mt-2 font-display text-5xl uppercase tracking-wider text-ink">
        Settings
      </h1>
      <p className="mt-4 max-w-xl text-fg-2">
        Budgets, salesperson and crew rosters, holidays, allowlist, and yearly target controls land here next.
      </p>
    </main>
  );
}
